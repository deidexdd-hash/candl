# Язык Пламени — Журнал исправлений
> Ведётся с момента первого запуска · Обновляется после каждого изменения

---

## Статус задач

| # | Задача | Статус | Версия |
|---|---|---|---|
| 1 | Убрать английский текст из интерфейса | ✅ Готово | v16 |
| 2 | Тест-режим — открыть все функции без оплаты | ✅ Готово | v17 |
| 3 | Robokassa — альтернативный способ оплаты | ⏳ Планируется | v18 |

---

## Задача 1 — Английский текст ✅

### Что было исправлено

| Файл | Проблема | Решение |
|---|---|---|
| `candle.ts` (backend) | Ответ содержал `moonPhase: "new_moon"` | Добавлено поле `moonPhaseRu: "Новолуние"` |
| `CandlePickPage.tsx` | В результате подбора показывался `new_moon` | Теперь показывает `moonPhaseRu` |
| `LibraryPage.tsx` | Глава показывала только `preview` (~100 символов) | Теперь показывает полный `body` с разбивкой по абзацам |
| `LibraryPage.tsx` | Tier-label был пустым для некоторых глав | Добавлены русские подписи `Практик и выше`, `Мастер и выше` |

---

## Задача 2 — Тест-режим 🔄

**Решение:** переменная окружения `TEST_USER_IDS` — список telegram_id через запятую.
Пользователи из этого списка автоматически получают тариф `master` при авторизации.

**Принцип работы:**
```
TEST_USER_IDS=123456789,987654321

POST /auth/telegram
  → если telegramId в TEST_USER_IDS
  → tier = master (без оплаты)
  → isTestUser = true в ответе
```

**Затрагиваемые файлы:**
- `apps/backend/src/routes/auth.ts`
- `render.yaml` — добавить `TEST_USER_IDS` в envVars

---

## Задача 3 — Robokassa ⏳

**Принцип работы Robokassa:**
1. Фронтенд запрашивает у бэкенда ссылку на оплату
2. Бэкенд формирует URL с подписью (MD5 хэш)
3. Пользователь переходит на страницу Robokassa и платит картой
4. Robokassa отправляет вебхук на бэкенд — бэкенд обновляет tier

**Нужно получить в личном кабинете Robokassa:**
- `ROBOKASSA_LOGIN` — логин магазина
- `ROBOKASSA_PASSWORD1` — пароль #1 (для создания платежей)
- `ROBOKASSA_PASSWORD2` — пароль #2 (для проверки вебхука)

**Затрагиваемые файлы:**
- `apps/backend/src/routes/payments.ts`
- `apps/frontend/src/pages/PaywallPage.tsx`
- `render.yaml`
- `.env.example`

---

## История версий

| Версия | Что изменилось |
|---|---|
| v1–v10 | Scaffold, структура, TypeScript, ESM→CJS |
| v11 | content.ts: require вместо dynamic import |
| v12 | Полная ревизия: все TS ошибки, data/ в dist/ |
| v13 | Отдельные файлы DiaryPage, ProfilePage; vite/client типы |
| v14 | render.yaml: db push вместо migrate deploy |
| v15 | App.tsx: fallback при пустом initData; VITE_API_URL в render.yaml |
| v16 | Английский текст → русский; chapter.body в библиотеке |
| v17 | Тест-режим: TEST_USER_IDS → автоматически tier=master |
| v18 | Robokassa (планируется) |

---

## Задача 4 — Новая глава из книги ✅ v18

Добавлена глава XIV «Церомантия и молибдомантия» из загруженного docx-файла.

| Параметр | Значение |
|---|---|
| ID | 22 |
| Part | 6 (Практики) |
| Tier | master |
| Символов | 6 676 |

**Содержание:** история 5000 лет (Египет, Греция, Русь, Финляндия, Норвегия), три метода отливки, финский метод чтения теней, пять уровней анализа, словарь символов, специальные ритуальные отливки, правила хранения форм.

**Что нужно обновить в репозитории:**
- `apps/backend/src/data/chapters.json` — новый файл из архива v18
